# Direct+ Deployment Environment Variables Template
# Copy these variables to Twilio Functions environment

# ====================================================================
# REQUIRED VARIABLES - Must be configured for deployment to work
# ====================================================================

# Email Configuration - REQUIRED for email notifications
ADMIN_EMAIL=admin@clientdomain.com
MAILGUN_DOMAIN=voicemail.clientdomain.com
MAILGUN_API_KEY=your-domain-specific-sending-api-key

# Multiple admin emails (separate with commas, no spaces)
# ADMIN_EMAIL=admin1@domain.com,admin2@domain.com,admin3@domain.com

# ====================================================================
# TWILIO STANDARD VARIABLES - Should already exist
# ====================================================================

# Core Twilio credentials (should already be configured)
ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
AUTH_TOKEN=your-auth-token-here

# TaskRouter configuration (should already be configured)
WORKSPACE_SID=WSxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
WORKFLOW_SID=WWxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Flex configuration (should already be configured)
DOMAIN_NAME=your-deployment-domain.twil.io

# ====================================================================
# OPTIONAL VARIABLES - For advanced configuration
# ====================================================================

# Custom email templates (optional)
# EMAIL_TEMPLATE_TYPE=standard
# ORGANIZATION_NAME=Client Organization Name
# EMAIL_SIGNATURE=Custom email signature text

# Email delivery options (optional)
# EMAIL_RETRY_ATTEMPTS=3
# EMAIL_TIMEOUT_SECONDS=30
# MAX_ATTACHMENT_SIZE_MB=25

# Voicemail configuration (optional)
# MAX_RECORDING_LENGTH=300
# ENABLE_TRANSCRIPTION=true
# TRANSCRIPTION_LANGUAGE=en-US

# Queue configuration (optional)
# QUEUE_TIMEOUT=300
# MAX_QUEUE_TIME=1800
# CALLBACK_TIMEOUT=86400

# ====================================================================
# DEPLOYMENT NOTES
# ====================================================================

# 1. Replace placeholder values with actual client configuration
# 2. Ensure MAILGUN_API_KEY is the domain-specific sending key, not private key
# 3. Test MAILGUN configuration before deployment:
#    curl -s --user "api:YOUR_MAILGUN_API_KEY" \
#        https://api.mailgun.net/v3/YOUR_MAILGUN_DOMAIN/messages \
#        -F from='Test <test@YOUR_MAILGUN_DOMAIN>' \
#        -F to='YOUR_ADMIN_EMAIL' \
#        -F subject='Pre-Deployment Test' \
#        -F text='Testing email configuration'
#
# 4. Verify WORKFLOW_SID matches "Assign to Anyone" workflow:
#    twilio api:taskrouter:v1:workspaces:workflows:list \
#        --workspace-sid YOUR_WORKSPACE_SID

# ====================================================================
# SECURITY REMINDERS
# ====================================================================

# - Never commit this file with real values to version control
# - Store sensitive values only in Twilio Functions environment variables
# - Rotate API keys quarterly
# - Use separate keys for development vs production
# - Monitor API key usage in Mailgun dashboard